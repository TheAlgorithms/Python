


def insertionSort(array):
    # Write your code here
	k=0
	for a in range(0,len(array)-1):
		for b in range(a+1,len(array)):
			if(array[a]>array[b]):
				k=array[b]
				array[b]=array[a]
				array[a]=k
	return array
