# these lines can be added as a optional for enhancement of project not necessary option.
# For greyscale:
# frame = cv2.cvtColor(frame, cv2.COLOR_RGB2GRAY)
# KINDLY apply threshold values for all pixels with a level larger than 80 are shown in white.
# And the others will be shown in black
# ret,frame = cv2.threshold(frame,80,255,cv2.THRESH_BINARY)
